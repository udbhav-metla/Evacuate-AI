# EVACUATE-AI Evacuation Graph

Agent-based microscopic evacuation simulation, computational egress graph, reproducible seeds, and 3D digital twin visualization for multi-floor buildings.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
